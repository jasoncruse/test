var fn = (num) => num + 2;

var promise = Promise.resolve('ok');

[1,2,3].includes(1);

[1, 4, -5, 10].find((item) => item < 0);

class Person {
    sayname() {
        return 'name'
    }
}
var john = new Person()
console.log(john)
